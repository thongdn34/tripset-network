# tripset-network
A small network
To run frontend:
  - cd to frontend folder
  - npm start
To run api:
  - cd api folder
  - npm start
To run both of them:
  - At root folder (tripset-network), run npm start
